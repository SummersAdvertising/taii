//remove link
$("<%= escape_javascript( "#dragablecontainer_#{@hqlevel.id.to_s}" ) %>").remove();
//remove rename form
$("<%= escape_javascript( "#renamelevelcontainer_#{@hqlevel.id.to_s}" ) %>").remove();