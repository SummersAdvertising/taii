//remove link
$("<%= escape_javascript( "#dragablecontainer_#{@brlevel.id.to_s}" ) %>").remove();
//remove rename form
$("<%= escape_javascript( "#renamelevelcontainer_#{@brlevel.id.to_s}" ) %>").remove();